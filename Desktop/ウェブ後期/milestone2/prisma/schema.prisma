// Prisma 6 用：ブログアプリの schema.prisma（完全版）

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Blog {
  id       Int     @id @default(autoincrement())
  title    String
  date     String
  content  String
}
